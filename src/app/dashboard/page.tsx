export default function DashboardPage() {
  return (
    <main style={{ padding: '2rem' }}>
      <h1>Dashboard — coming soon</h1>
      <p style={{ color: 'var(--text-muted)', marginTop: '0.5rem' }}>
        DB and GitHub wired up. Build overview next.
      </p>
    </main>
  );
}
